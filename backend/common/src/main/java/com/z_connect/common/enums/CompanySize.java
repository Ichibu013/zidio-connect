package com.z_connect.common.enums;

/**
 * Enum to represent the different sizes of a company.
 * Corresponds to the ENUM('STARTUP', 'SMALL', 'MEDIUM', 'LARGE', 'ENTERPRISE') type in the database.
 */
public enum CompanySize {
    STARTUP,
    SMALL,
    MEDIUM,
    LARGE,
    ENTERPRISE
}
